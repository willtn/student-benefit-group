{
  "name": "student-benefit-group",
  "version": "0.1.2",
  "description": "Student Benefit Group for Student Benefit",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf build/**/* && tsc -p src/tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "rimraf dist/**/* && npm run build && cp -r build/* dist/"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.pdx.groowin.com:trung.nguyen/student-benefit-group.git"
  },
  "author": "Trung Nguyen",
  "private": true,
  "devDependencies": {
    "rimraf": "^2.6.1",
    "typescript": "^2.6.1"
  },
  "dependencies": {
    "address": "git+http://gitlab.pdx.groowin.com/trung.nguyen/address.git",
    "funfix-core": "^6.2.2"
  }
}
